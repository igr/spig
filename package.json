{
  "name": "spig-example",
  "version": "1.6.0",
  "description": "SpigExample",
  "scripts": {
    "clean": "gulp clean",
    "dev": "gulp dev",
    "serve": "gulp build && gulp serve",
    "build": "gulp build",
    "build:lambda": "cd netlify && build.sh"
  },
  "dependencies": {
    "spig": "file:./spig"
  }
}
