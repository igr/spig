# Build with Spig

## Build

Build:

```shell
yarn build
```
or
```
gulp build
```

The output folder is `out`.

## Development mode

Development mode, watch & server on port `3000`:

```shell
yarn dev
```
