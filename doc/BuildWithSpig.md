# Build with Spig

## Build

Build:

```shell
yarn build
```

or

```shell
gulp build
```

The output folder is `out`.

Clean:

```shell
yarn clean
```

## Development mode

In development mode, watch & server on port `3000`:

```shell
yarn dev
```
