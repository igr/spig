---
title: "All Tags"
---

<ul>
  {% for tag in site.tags %}
    <li>
      {% set t = site.pageOfCollection('tags', tag) %}

      <a href="{{ t.url }}">{{ tag }}</a>
      <ul>
        {% for tagPage in site.collections.tags[tag] %}
          <li><a href="{{ tagPage.url }}">{{ tagPage.title }}</a></li>
        {% endfor %}
      </ul>
    </li>
  {% endfor %}
</ul>
